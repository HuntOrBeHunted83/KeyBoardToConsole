

keyboard to console mapping 
    Mapping should be stored in local 
    Read it at startup 
        If not present, there should be a default 
    

Fixed Mappings
    WASD → left stick,
    mouse movement (via Pointer Lock) → right stick

Configurable
    configurable keys/clicks → buttons/bumpers/triggers/D-Pad